# -----------------------------
# FONT (clean + readable)
# -----------------------------
font_family      JetBrainsMono Nerd Font
bold_font        auto
italic_font      auto
font_size        14

# -----------------------------
# APPEARANCE (Konsole-like)
# -----------------------------
background_opacity 0.92
cursor_shape beam
cursor_blink_interval 0.5

# Smooth scrolling
scrollback_lines 10000

# -----------------------------
# COLORS (safe dark theme)
# -----------------------------
foreground #d0d0d0
background #0e0e0e

selection_foreground #000000
selection_background #ffffff

# ANSI colors
color0 #000000
color1 #cd0000
color2 #00cd00
color3 #cdcd00
color4 #0000cd
color5 #cd00cd
color6 #00cdcd
color7 #e5e5e5
color8 #7f7f7f
color9 #ff0000
color10 #00ff00
color11 #ffff00
color12 #5c5cff
color13 #ff00ff
color14 #00ffff
color15 #ffffff

# -----------------------------
# TABS (like Konsole)
# -----------------------------
enable_tab_bar yes
tab_bar_edge top
tab_bar_style powerline

# -----------------------------
# KEYBINDINGS (THIS IS THE MAGIC)
# Konsole-like feel
# -----------------------------

# New tab (like Konsole Ctrl+Shift+T)
map ctrl+shift+t new_tab

# Close tab
map ctrl+shift+q close_tab

# Next / Previous tab
map ctrl+tab next_tab
map ctrl+shift+tab previous_tab

# Next / Previous tab with Ctrl+Shift+Right/Left
map ctrl+shift+right next_tab
map ctrl+shift+left previous_tab

# Split (like Konsole)
map ctrl+shift+d launch --location=vsplit
map ctrl+shift+enter launch --location=hsplit

# Navigate splits
map alt+left neighboring_window left
map alt+right neighboring_window right
map alt+up neighboring_window up
map alt+down neighboring_window down

# Resize splits
map ctrl+alt+left resize_window narrower
map ctrl+alt+right resize_window wider
map ctrl+alt+up resize_window taller
map ctrl+alt+down resize_window shorter

# Copy paste (normal behavior)
map ctrl+shift+c copy_to_clipboard
map ctrl+shift+v paste_from_clipboard

# Close only the current split (window)
map alt+q close_window

# -----------------------------
# PERFORMANCE
# -----------------------------
repaint_delay 10
input_delay 3
sync_to_monitor yes

# -----------------------------
# SHELL (your zsh)
# -----------------------------
shell /usr/bin/zsh
